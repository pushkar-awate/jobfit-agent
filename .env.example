# Optional. Export this to use a hosted LLM brain (free tier at groq.com).
# Without it, the agent runs on the deterministic mock brain with zero setup.
#   export GROQ_API_KEY=your_key_here
GROQ_API_KEY=
