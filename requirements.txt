# The jobfit-agent core (agentcore + CLI) is pure Python standard library.
# The live job-board web UI (streamlit_app.py) needs Streamlit:
streamlit>=1.30
# Optional: set GROQ_API_KEY to use a hosted LLM brain instead of the mock brain.
